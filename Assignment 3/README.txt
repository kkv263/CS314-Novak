Kevin Vu (kkv263)
CS314 Data Structures
September 2015

Instructions to run file.

javac Cons.java
java Cons

Questions to Assignment 3:

4. The function elt will run n times, so the order is O(n). However this is not as efficient
   as array access because the order of array access is O(1).

5. The interpolate function creates a bell shaped curve for binomial(12) It increases at a
   decreasing rate until the peak. The graph then mirrors the left side of the peak to
   form the right side of the curve.